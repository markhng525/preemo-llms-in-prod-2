import os

# Change the default in the case that this is different in your project repo
HOME_PATH = os.environ.get("HOME", "/home/markh/preemo-llms-in-prod-conf2")
PROJ_ROOT = f"{HOME_PATH}/preemo-llms-in-prod-conf2"
